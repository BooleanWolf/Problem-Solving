#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    clock_t st = clock(); 

    long long int testcase;
    cin >> testcase; 
    
    while(testcase--){
        int a = 1; 
    }

    clock_t en = clock(); 

    cout << 1.0 * (en - st) / CLOCKS_PER_SEC  << '\n'; 
    return 0;

}
